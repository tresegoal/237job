/**
 * Created by MTM on 02/04/2018.
 */

$/*(function () {
    var filtre = {
        initialize: function () {
            $('a[data-method]').on('click', this.handleMethod);
        }
        handleMethod: function (e) {
            e.preventDefault();
            var link = $(this),
                httpMethod = link.data('method').toUpperCase(),

        }
    };
})*/
 
function filtreType() {
    alert('vous avez cliquer');
}